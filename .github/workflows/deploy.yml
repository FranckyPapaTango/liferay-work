name: Deploy liferay-work (Docker)

on:
  push:
    branches:
      - master

  workflow_dispatch:

concurrency:
  group: deploy-master
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout complet du dépôt
#      - name: Checkout repository
#        uses: actions/checkout@v4
#        with:
#          fetch-depth: 0
#
#      # 2. Debug commit
#      - name: Debug commit
#        run: |
#          echo "Branch: $GITHUB_REF"
#          echo "Commit SHA: $GITHUB_SHA"
#          git log -1
#
#      # 3. Setup SSH agent
#      - name: Setup SSH
#        uses: webfactory/ssh-agent@v0.8.0
#        with:
#          ssh-private-key: ${{ secrets.DEPLOY_KEY }}
#
#      # 4. Ajouter le serveur aux known_hosts
#      - name: Add server to known_hosts
#        run: |
#          mkdir -p ~/.ssh
#          ssh-keyscan -4 -H ${{ secrets.DEPLOY_HOST }} >> ~/.ssh/known_hosts || true
#
#      # 5. Synchronisation du projet vers le serveur
#      - name: Sync project
#        run: |
#          rsync -avz --delete \
#            --exclude=node_modules \
#            --exclude=.git \
#            --exclude=uploads \
#            ./ \
#            ${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }}:/opt/Madaskill
#
#      # 6. Build & run Docker containers (OPTIMIZED ULTRA SAFE)
#      - name: Build & run containers
#        run: |
#          ssh ${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }} <<'EOF'
#            set -e
#            cd /opt/Madaskill
#
#            echo "=============================="
#            echo "Server time:"; date
#            echo "=============================="
#
#            echo "Docker version:"; docker version
#            echo "Docker compose version:"; docker compose version
#            echo "=============================="
#
#            echo "Stopping containers if running..."
#            docker compose down || true
#
#            echo "Cleaning Docker cache..."
#            docker builder prune -af
#            docker system prune -af
#
#            echo "Building images WITHOUT cache..."
#            docker compose build --no-cache
#
#            echo "Starting containers (force recreate)..."
#            docker compose up -d --force-recreate
#
#            # <-- pause 5s pour s'assurer que les containers démarrent complètement
#            sleep 5
#
#            echo "Containers status:"
#            docker compose ps
#            echo "=============================="
#          EOF
